export class Personne {
    constructor(
        public name: String, public phoneNumber: string, public email: string, public location: string, public password: String, public id?: number) {

    }
}
